# 可视化日记程序 需求文档

> 版本：v3.2（2026-09-06 三次增补：R3 层级挂载放宽 + 弹窗层级修复；v3：R1 逐级标签选择、R2 日内状态转变）
> 历史：v2 定稿一期范围；v1 初始讨论。
> 状态：已与需求方逐条讨论确认，作为一期开发依据。

## 1. 产品定位

个人使用的 macOS 桌面"可视化日记"程序：以**可量化的活动记录**（三级分类 + 时长）为主体，辅以自由文字；支持按日/周/月/年做多级下钻的数据可视化，并可将任意一天导出为手帐风格长图。

## 2. 形态与架构

| 项 | 决策 |
|---|---|
| 平台 | 仅 macOS 桌面；网页装成 App（PWA 安装进 Dock，独立窗口无地址栏） |
| 架构 | FastAPI 后端 + SQLite 本地单文件库 `data/diary.db` + 原生 Web 前端（ECharts 本地引入） |
| 启动 | 一期：手动启动脚本 `start.sh`；二期：launchd 开机自启 |
| 视觉 | App：深色底 + 玻璃质感（glassmorphism）卡片 + 莫兰迪色板 + 极简；导出长图：手帐纸质感，支持深浅两色 |

## 3. 数据模型

```
分类节点 Category：树形三级（领域 > 活动类型 > 具体对象），每级均可增删改（改名/删除/同级合并）；
                   初始模板内置到第 2 级，必含：工作、娱乐、出行、食物；
                   预置可删项：健康运动、学习成长、社交、日常事务。
                   第 3 级标签在使用中随手新建（如输入新游戏名自动注册）。
活动记录 Entry    ：日期(YYYY-MM-DD) + 分类标签(第3级) + 时长(必填，分钟精度存储)
                   + 起止时间(选填, HH:MM) + 备注文本(选填)
每日页 DayPage    ：自由文字 + 心情转变序列 + 天气转变序列
                   （序列 = 最多 5 项的有序列表，每项 {值, 可选时刻 HH:MM}，
                     支持"晴 → 雨"这类日内变化；v3-R2 取代原单值字段）
```

### 3.1 v3 增补需求（2026-09-06 确认）

**R1 标签逐级独立选择**
- 录入器的分类选择改为 **一级 / 二级 / 三级 三个独立下拉**，未选上级时下级禁用并提示"先选上一级"；选定一级后，二级下拉只显示该一级的子级（三级同理）；
- 三个级别均为"可搜索下拉"：输入即过滤现有选项，点击选择；
- **输入即新建**：输入的内容与当前级别所有选项不同、回车确认后自动创建（一级挂根、二级挂已选一级、三级挂已选二级，颜色继承大类）并选中；同名（同父级下）则直接选中已有项；
- 一级"领域"同样支持搜索+新建（与设置页能力一致，三级统一行为）；
- "最近使用"快捷 pill 保留：点一下 = 三列同时填好；编辑记录时三列自动回填；
- 设置页分类管理同样**逐级进入管理**：一级列表 → 点"进入"看其二级 → 再进入看三级，各级独立增删改/合并，面包屑导航返回；
- 新建的一级领域自动从备用莫兰迪色板取色，保证颜色链（L1→L2→L3→记录/时间轴/图表）完整。

**R2 日内状态转变**
- 心情、天气各自为有序序列，**最多 5 项**（前后端都校验，超限 400）；
- 每项 = 状态值 + **可选时刻**（HH:MM），如 `☀️ 08:00 → 🌧 14:00`；
- UI 为"转变链"：点项编辑、🗑 删除、尾部"＋ 转变"追加（满 5 隐藏）；
- 旧单值 mood/weather 字段**直接替换**（`mood_json`/`weather_json` 列），不兼容旧数据（升级时删除 `data/diary.db` 重建即可）；
- 状态与活动记录**不做时段关联**（仅呈现流程）；导出长图头部渲染整条转变链。

**R3 层级挂载放宽（v3.2 追加）**
- 活动记录可挂在分类树 **1/2/3 任一级**（如只选一级「健康运动」或二级「睡眠」即可保存，无需强制三级）；统计与最近使用覆盖各级；
- 状态编辑弹窗层级高于卡片焦点态，任何入口打开不被遮挡。

统计口径（5 条，均已确认）：
1. 一天记录总时长不要求凑满 24h，未记录即空白；
2. 时间轴上的空隙留白，不猜测内容；
3. 跨午夜记录归属**开始那天**；
4. 同一天同一标签允许多条记录，统计自动求和；不做时段重叠校验；
5. 存储精确到分钟，图表展示粒度 15 分钟。

## 4. 功能清单（一期）

### A. 今日页（默认首页）
- 打开即"今天"；标签选择按 R1 逐级进行（选一级→选二级→选三级/输入新建），配合时长快捷芯片与 ⏎，常规路径 3~4 次点击完成一条记录；
- 每日自由文字、心情/天气按 R2 记录为日内转变链（最多 5 段）。

### B. 历史日期浏览与记录
- **日历页**：月视图网格，每格显示日期 + 当天记录概要（彩色标记/时长），可翻任意月份、一键回今天；
- **任意日期直达**：点击日历任一格（无论有无记录）进入该日详情页；
- **详情页 = 今日页的历史形态**：同一组件由日期参数驱动，布局一致，处于可编辑态，可新增/修改/删除该日记录；
- **快速导航**：前一天/后一天箭头 + 日期直跳 + "回到今天"；
- 未来日期可查看（空页），入口弱化。

### C. 每日时间轴
- 有起止时间的记录渲染为横向色块分带（0:00–24:00），颜色随所属大类，空隙留白；今日页与任意历史日均有。

### D. 统计页（ECharts）
- **旭日图**：三级占比，点击逐层下钻 + 面包屑返回；
- **堆叠柱状图**：X 轴按日/周/月/年聚合，柱内按大类分色；
- **日历热力图**：颜色深浅 = 当日记录总时长；点击某天跳转到 B 的历史详情页。

### E. 导出 PNG 手帐长图
- 任意日期可导出；内容顺序：日期+天气心情 → 时间轴色带 → 活动清单（分类路径+时长+备注）→ 迷你时间分布饼图 → 今日随想；
- 手帐纸感风格，深浅两色版本；架构上为未来 PDF 留出扩展余地（一期不做 PDF）。

### F. 分类管理页
- 按 R1 逐级进入管理（一级列表 → 二级列表 → 三级列表），各级独立增删改、改名、同级合并，面包屑返回。

### G. 备份/恢复
- 一键导出 JSON（含分类树 + 全部记录 + 每日页），可从 JSON 恢复。

### 明确不做
时间目标/监督、PDF 导出、多端同步、账号系统。

## 5. 实施阶段

```
阶段0  文档落盘：需求文档.md + .venv 初始化
阶段1  数据层：SQLite schema + 模板种子 + CRUD/按日读取 API（pytest 验证）
阶段2  记录动线：今日页 + 任意日期详情页（同一组件、日期驱动）+ 标签录入
       + 心情天气文字 + 每日时间轴
阶段3  历史浏览：日历月视图 + 日期导航 + 热力图↔详情页跳转打通
阶段4  统计：旭日图/堆叠柱/热力图 API 与前端渲染（数值与手算核对）
阶段5  导出：手帐 HTML 模板 → PNG 长图（深浅两色，任意日期）
阶段6  收尾：深色玻璃拟态莫兰迪主题打磨 + 备份/恢复 + PWA manifest
       + 启动脚本 + 开机自启
```

每阶段完成后更新 `工作内容总结.md`。

- 统计口径不变；升级 v3 时本地库结构变化（day_pages 列替换），删除 `data/diary.db` 重启自动重建（开发期无真实数据，已确认可接受）。

## 6. 技术约定

- 所有依赖安装与运行必须在根目录 `.venv` 虚拟环境中进行；
- 前端图表库 ECharts、导出截图 html2canvas 均以本地 vendor 文件引入（离线可用）；
- 编码规范遵循根目录 `coding.md`：最小实现、不做需求外功能、改动可追溯到需求条目。
