# 工作内容总结

## 2026-09-06 · 可视化日记程序 一期开发（阶段 0–6 完成）

### 需求确认（Plan 阶段逐条对齐，定稿见 `需求文档.md` v2）
- 形态：仅 macOS，网页装成 App（PWA 进 Dock）；FastAPI + SQLite + 原生前端（ECharts/html2canvas 本地 vendor，离线可用）；
- 数据：三级分类树（模板内置到二级，三级随用随建）、活动记录（时长必填+起止时间选填）、每日页（文字/心情/天气）；
- 统计口径 5 条（不强制 24h、空隙留白、跨午夜归开始日、同日同标签可多条、存分钟展 15 分钟粒度）；
- 功能：今日页 + **历史日期浏览与任意日期记录**（日历月视图/前后翻页/直跳）、时间轴色带、旭日图/堆叠柱/热力图、PNG 手帐长图（深浅纸感）、分类管理（含合并）、JSON 备份恢复；
- 视觉：App 深色 + 玻璃质感 + 莫兰迪色；导出图手帐纸感。

### 交付物
```
需求文档.md                 # v2 定稿
backend/                  # FastAPI
  app.py                  # 入口：建表+种子+挂路由+静态托管
  database.py / seed.py / helpers.py
  routers/ categories.py（树/CRUD/merge/recent） entries.py（任意日期读写/新标签自动注册）
          stats.py（sunburst/trend/calendar/heatmap） backup.py（导出/事务恢复）
frontend/                 # 深色玻璃拟态 SPA（hash 路由）
  index.html manifest.webmanifest icon.svg css/style.css
  js/: api util timeline composer dayview calendar stats export settings app
tests/test_api.py         # 13 个用例全部通过（含历史日期、跨午夜、合并、备份回滚）
start.sh requirements.txt .gitignore
data/diary.db             # 本地单文件数据库
```

### 运行方式
- 启动：`./start.sh`（或 `source .venv/bin/activate && python -m uvicorn backend.app:app --port 8730`）→ 浏览器开 http://localhost:8730
- 装成 App：Chrome/Edge 地址栏右侧「安装」按钮（或设置页浮层），独立窗口进 Dock；
- 测试：`source .venv/bin/activate && python -m pytest tests -q`。

### 验证记录
- pytest 13/13 通过；uvicorn 实跑：模板种子、新标签「博德之门3」自动注册到 娱乐›游戏、二级拒绝直挂、日详情/旭日图返回正确；前端 9 个 JS 均通过 node --check；静态资源 200。

### 待办 / 已知边界
- 开机自启 launchd 脚本（用户确认二期做）；
- 导出长图依赖浏览器下载，未做真实浏览器端到端截图验证（建议首次使用时人工看一张导出图）；
- 趋势图年粒度桶标签为 YYYY，跨年对比时按字符串排序即可。

---

## 2026-09-06 · 需求 v3 增补开发（R1 逐级标签选择 + R2 日内状态转变）

### 需求增量（用户提出，逐条确认后写入需求文档 §3.1）
- **R1**：分类选择每级独立——录入器改为一级/二级/三级三个可搜索下拉，选定上级才显示/启用下级；每级输入新内容回车即自动新建并选中（同名则选中已有），一级同样支持新建；编辑与"最近使用"自动回填三列；设置页分类管理改为逐级进入（面包屑导航），各级独立增删改/合并；
- **R2**："此刻状态"支持日内变化——心情/天气各为有序序列（最多 5 段），每段 = 值 + 可选时刻，UI 呈"☀️ ·08:00 → 🌧 ·14:00"转变链，可增/改/删；后端 `mood_json`/`weather_json` 替换旧单值列（不做旧数据兼容），5 段上限前后端双重校验（超限 400）；状态与活动记录不做时段关联；导出长图头部渲染转变链。

### 改动清单
```
backend/  database.py（day_pages 列替换） helpers.py（check_states + DayPageIn 列表化）
          routers/entries.py（状态 JSON 读写） routers/categories.py（新建一级自动取备用色）
          seed.py（EXTRA_PALETTE）
frontend/ js/ss.js（新增：可搜索下拉组件，输入即新建/同名即选中）
          js/composer.js（重写为三列级联，未选上级禁用+提示，编辑回填，保存按钮联动点亮）
          js/dayview.js（状态转变链 UI） js/settings.js（逐级进入式分类管理）
          js/export.js + css/style.css（导出链样式、级联/链条样式）
tests/    状态序列往返/上限/格式校验、逐级新建挂载、新一级配色 —— 16 用例全过
需求文档.md 升 v3；本地库已重建（v2→v3 结构迁移）。
```

### 2026-09-06 · 阶段A/B/C：保存链路修复 + 级联删除 + Finder 式分类树

#### 用户反馈问题与根因
1. **新建/编辑记录点保存无效（按钮不亮）**：时长快捷芯片 `+15m/+30m/…` 的点击处理漏调 `syncSave()`，用手输时也只在 `onchange`（失焦）触发 → 芯片加时长场景按钮永远置灰。修复：芯片/清零/手输（`oninput` 实时）/三列选择全部联动；输入框 `type=search→text` 去掉原生清除键干扰；保存成功后录入器整体重置。
2. **删除母级被拒**：原后端"有子级/有记录即 409"。改为**级联删除**：`DELETE /api/categories/{id}` 事务内删除整棵子树 + 其下全部记录，返回删除统计；新增 `GET /{id}/subtree-size` 预览将删除的数量。
3. **分类管理交互**：由"逐级进入"改为 **macOS Finder 式原位展开树**——▸/▾ 展开收起（状态跨渲染保留）、层级缩进、行悬停高亮，各行保留 ＋子级/✏️改名/⇄合并/🗑删除；删除前 confirm 弹出"将删除 N 个分类 / M 条记录"，删除后 toast 反馈并清理展开状态。

#### 验证
- pytest **17/17**（新增级联删除用例：3 级子树+2 条记录一并删除、兄弟分类与无关日期不受影响）
- 实跑：subtree-size 预览准确；级联删除后今日记录清零；误删模板后 restore 清空重建正常
- E2E 回归 **43/43**；全部 JS `node --check` 通过

> 教训记录：上一轮"E2E"只覆盖 API 层，未覆盖前端交互（按钮联动），导致保存 bug 漏检。本轮起前端交互问题以"代码走查 + 用户手测清单"双重把关。
>
### 2026-09-06 · 运维脚本：start.sh / stop.sh

- **start.sh 改造**：后台运行（nohup）+ PID 写入 `.server.pid`；重复启动自动检测并提示"已在运行"；端口被占时给出指引；启动失败自动回显日志尾部并清理 pid 文件。**前台运行需求可临时用** `source .venv/bin/activate && python -m uvicorn backend.app:app --port 8730`。
- **stop.sh 新增**：优先按 pid 文件优雅停止（TERM，最多等 5s，超时 KILL），并清理 pid 文件；兜底清理占用 8730 端口的残留进程（覆盖手动启动的场景）；未运行时友好提示。
- 实测闭环：未运行提示 → 启动(200) → 重复启动拦截 → 停止(端口关闭) → 再启动(200)。`.server.pid` 已在 .gitignore。

按用户反馈"布局/渲染不够好看"，完成 7 组精修（提交 d5e05eb）：
1. **设计令牌体系**：8pt 间距网格（--sp-1~6）、字号阶梯（12/13/15/18/24/32）、圆角家族（8/12/18/full）、主色三件套（normal/hover/active）+ 选中态填充/焦点环；
2. **色彩**：背景改同色相深浅渐变（#141519 底 + 蓝灰/灰紫两团），消除发灰感；次要文字提亮至 #a5a29a（对比度达标不糊）；弱提示 #7d7a74 三级分层；
3. **卡片层次**：主卡片 7% 白面 + 投影 + 顶部内亮边（玻璃厚度暗示），"今日随想"为次要卡片（4% 白无投影）形成前后景深；
4. **排印**：日期主标题 32px/700（原 20/600）；卡片标题统一"小节标签"风格（12px/600/字距2px/次要色）；活动时长加粗 + 全局数字等宽；
5. **控件三态**：chip 选中改 accent 14% 填充+描边；按钮 hover/active 按压/focus ring；nav 选中项加 macOS 式左侧 3px 绿色指示条；
6. **微交互**：记录行/日历格 hover 上浮 1px+投影；页面切换 150ms 淡入；toast 弹性上浮；今日页日期导航加副标题提示语；
7. **图表主题统一**：ECharts 文字/网格线/tooltip 背景与 CSS 令牌同源；tooltip 显示格式化时长（1h30m 而非 90）；堆叠柱圆角顶+限宽；旭日图 hover 阴影；热力图色阶起点更贴底色；日历格 4px 圆角；
8. **日历徽章化**：有记录日显示 accent 时长徽章（替代"2 条 · 2h"纯文字）、日期加粗、未来日虚线弱化、今天格 accent 14% 底色。

验证：全部 JS node --check 通过、pytest 17/17、旧令牌（--glass/--text-dim/--radius）零残留、页面 200。





### 2026-09-06 · v3.2 修复与放宽（用户反馈两点）
1. **状态弹窗被卡片遮挡**：根因是上一轮修下拉遮挡时给 `.card:focus-within` 加了 z-index:50，而弹窗遮罩只有 z-index:40，且弹窗打开时卡片内时间输入框抢焦点触发 50，弹窗反而被压在下面。修复：遮罩层级提到 100（高于卡片焦点态），心情/天气两个入口一并解决。
2. **记一条放宽为至少选一级即可保存**（睡眠等没有子级的场景）：
   - 后端 entries 创建/编辑不再强制三级，1/2/3 任一级都合法；
   - 前端保存按钮条件改为选到任一级+有时长，挂载点取已选的最深层（l3||l2||l1）；
   - 编辑回填 fillFromCatId 支持记录挂在 1/2 级的反查填充；三级下拉 placeholder 标注可跳过；
   - recent 端点 LEFT JOIN 放宽到各级分类（一级直挂也能进最近使用）。
   - 验证：pytest 18/18（新增 test_entry_on_any_level：一级/二级直挂、编辑改层级、旭日图聚合 480 分钟、recent 含一级）；实跑 e2e 全链路 OK 并清理数据；JS 语法全过。


### 2026-09-06 · v3.3 图标统一（用户反馈：初始与新增标签图标表现不一致）
- 根因：图标渲染规则五处各异（下拉只看自身/三级回退父级/记录列表取祖先最后/设置页空白/recent 兜底），初始标签与新建标签显示不一致。
- 定稿规则（用户拍板）：只有大类（一级）有图标；二三级不存、不显示、不继承、不做自定义。
- 实施：seed 模板 L2/L3 去图标；新建 L1 自动从 ICON_POOL 分配、子级强制 NULL；记录输出 icon 字段改 root_icon（仅一级）；前端六处统一为「一级带图标、其余纯名称+色点」；存量库迁移执行。
- 验证：pytest 19/19（新增 test_icon_only_on_root）；实跑 API 确认 path 无 icon、root_icon 正确、recent 子级 icon=None；e2e 回归通过；JS 全过。
